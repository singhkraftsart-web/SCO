import type { Metadata } from 'next';
import { absoluteUrl } from '@/lib/site';
export const dynamic = 'force-dynamic';
export const metadata: Metadata = { title: 'Admissions Admin Dashboard', robots: { index: false, follow: false }, alternates: { canonical: absoluteUrl('/admin') } };
async function getLeads() { try { const res = await fetch(absoluteUrl('/api/leads'), { cache: 'no-store' }); return (await res.json()).leads as Array<Record<string, string>>; } catch { return []; } }
export default async function AdminPage() { const leads = await getLeads(); return <section className="section"><div className="mx-auto max-w-7xl container-px"><p className="text-sm font-black uppercase tracking-[.24em] text-sky">Lead Generation</p><h1 className="mt-3 text-4xl font-black text-navy dark:text-white">Admissions Admin Dashboard</h1><p className="mt-4 text-slate-600 dark:text-slate-300">Production deployments should protect this route with authentication. Leads are stored in MongoDB when MONGODB_URI is configured.</p><div className="mt-8 overflow-hidden rounded-3xl border border-slate-200 dark:border-white/10"><table className="w-full text-left text-sm"><thead className="bg-ice dark:bg-slate-900"><tr><th className="p-4">Name</th><th className="p-4">Phone</th><th className="p-4">Goal</th><th className="p-4">Time</th><th className="p-4">Type</th></tr></thead><tbody>{leads.map((lead, i) => <tr className="border-t border-slate-200 dark:border-white/10" key={`${lead.phone}-${i}`}><td className="p-4">{lead.name}</td><td className="p-4">{lead.phone}</td><td className="p-4">{lead.goal}</td><td className="p-4">{lead.time}</td><td className="p-4">{lead.kind}</td></tr>)}</tbody></table></div></div></section>; }
